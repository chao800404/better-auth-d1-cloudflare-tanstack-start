name: Deploy and Initialize D1

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Workers
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Generate Better Auth schema
        run: pnpm auth:generate

      - name: Generate database migrations
        run: pnpm db:generate

      - name: Build
        run: pnpm build

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

      - name: Initialize D1 Database
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🔍 Finding D1 Database ID for 'better-auth-db'..."

          # List databases and fetch ID for better-auth-db
          DB_ID=$(npx wrangler d1 list --json | jq -r '.[] | select(.name == "better-auth-db") | .uuid')

          if [ -z "$DB_ID" ] || [ "$DB_ID" == "null" ]; then
            echo "❌ Error: Could not find database 'better-auth-db'"
            echo "Available databases:"
            npx wrangler d1 list
            exit 1
          fi

          echo "✅ Found Database ID: $DB_ID"

          # Update wrangler.jsonc or create a temporary config
          echo "🔧 Updating wrangler.jsonc with actual ID..."
          sed -i "s/YOUR_DATABASE_ID/$DB_ID/g" wrangler.jsonc

          echo "🚀 Applying D1 migrations..."
          npx wrangler d1 migrations apply DATABASE --remote

      - name: Deployment complete
        run: |
          echo "✅ Deployment successful!"
          echo "✅ D1 database initialized with all tables"
          echo "🚀 Your app is live!"
